# Assets

Toolbar icons are `icon-*.png` (and a vector `icon.svg`). The sun/moon washes (`wash-light.png`, `wash-dark.png`, `art-*.png`) ship in the packed add-on.

Sideload `dist/daylight.xpi` — that zip already contains every image. Rebuild it with `./scripts/pack.sh`.
